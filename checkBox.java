//import java.util.*;

public class checkBox {
    public static boolean ischeack(String s1, String s2) {
        // Arrays.sort(s1);
        // Arrays.sort(s2);
        if (s1.equals(s2)) {
            return true;

        }

        return false;
    }

    public static void main(string args[]) {
        String s1 = "noon";
        String s2 = "oonn";
        System.out.println(ischeack(s1, s2));

    }

}
